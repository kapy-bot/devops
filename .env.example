# Copy this file to .env before running docker compose up.
# .env itself is gitignored so real secrets never get committed --
# this file only documents which variables are needed.

POSTGRES_USER=todo
POSTGRES_PASSWORD=todo
POSTGRES_DB=todo
